export type LimitFormValues = {
    price: any;
    quantity: any;
    total: any;
}
export type MarketFormValues = {
    orderValue?: any;
    amount?: any;
}
export type OpenOrderFormValues = {
    currentPage: Number,
    nextPage: boolean,
    limit: number,
    count: number,
    data: any
}